# pi 在装包时会跑 npm install。npm 7+ 会自动安装 peerDependencies，
# 于是把 pi 的核心包（pi-ai / pi-tui / pi-agent-core / jiti 等）整套下载一份，约 158 MB、15000+ 个文件。
#
# 但那份副本根本不会被用到：pi 加载扩展时用 jiti 的 alias 把
# @earendil-works/* 和 typebox 指向它**自带**的副本（见
# packages/coding-agent/src/core/extensions/loader.ts 的 getAliases()）。
#
# 所以关掉 peerDeps 自动安装，装包体积从 ~158 MB 降到接近 0，功能不受影响。
legacy-peer-deps=true
