# 正在跑的实验：一行一条
#
# 格式：<服务器绝对路径> [Tab 或空格] <进程号>
#   进程号可选，但强烈建议填（nohup 起的话就是 echo $! 的值）：
#   填了才能在崩溃时立刻发现，不填只能等 72 小时超时。
#   Slurm / Docker 场景拿不到进程号就留空。
#
# 起完实验调 track_run 登记，处理完把这行删掉。
#
# 例：
# /data/you/proj/outputs/exp12	88231
# /data/you/proj/outputs/exp13
