# 自主科研迭代循环

> 本文件每轮都会进入上下文。下面的「服务器与实验」是**系统能否运转的关键**——
> table 模式完全依赖你（agent）配合，漏一步实验就永远不会被等，而且没有任何报错。

## 背景

<!-- TODO: 用一两句话写清你在做什么、当前阶段的目标 -->

## 每轮开始必须做

1. 读 `.auto/notes.md` —— **先看当前重心在哪一边**，再决定读哪个 goal
2. 读 goal（**每次都要重读，我可能刚改过**）—— 默认 `.auto/goal.md`，
   按工作流拆开的话读对应的那份（见下面的表）
3. 看 `.auto/runs.csv` —— 有哪些实验在跑；`track` / `note` 列写着每条属于哪边、在试什么
4. **需要回头看之前试过什么、或想回溯到某个节点时**，才读 `tree.md`
   （它记录了每一次尝试和对应的 commit，会一直变长，**不要每轮读**）

## 工作流（迭代 / baseline）

这个项目可能同时有两件事在跑。它们**共用一份 `.auto/runs.csv`**，用 `track` 列区分，
各自的 goal 和进度树用文件名分开：

| 工作流 | goal | 进度树 | track 值 |
|---|---|---|---|
| **默认（只有一个工作流）** | `.auto/goal.md` | `.auto/tree.md` | 留空 |
| 迭代 | `.auto/goal-iter.md` | `.auto/tree-iter.md` | `iter` |
| baseline | `.auto/goal-baseline.md` | `.auto/tree-baseline.md` | `baseline` |

`/rl setup` 生成的是**默认那份** `.auto/goal.md`。要按工作流分开就自己改名或新建，
**并把上面这张表改成你实际的**——这张表是给 agent 的索引，它照着读。

**怎么分取决于本地放不放代码**，两种都合理：

- 本地**不放**代码（这份只当控制台用）→ 用文件名区分（如上表），建子目录只是空壳
- 本地**放**代码 → 用子目录分开（`iter/` `baselines/`），各自带自己的 `.auto/`

分开的目的是**读的时候少带无关内容**，不是为了让目录好看。
服务器上代码放哪随意——有 `note` 描述，路径写进表就能找到。

**为什么要共用一份表**：拆成两份就得跑两个循环（两个 pi 实例），而它们**互相看不见**——
会抢同一批卡，而且一边看不到另一边的结果。共用一份，一个 agent 就能自己协调。

当前重心（先做哪边）写在 `.auto/notes.md` 开头——那个文件是整个项目的**调度层**。

## 每一轮怎么工作

这是一个**不停的迭代循环**——没有"做完"这回事，除非我叫停（Esc）。

你的目标不是"把实验跑完"，而是**让方法和代码持续变好**。实验只是验证手段，
它存在的意义是告诉你这次改得对不对。

一轮的节奏：

1. **看上一轮的结果**（`DONE` / 日志），判断这次改动值不值
2. **想清楚下一步改什么** ← 这一步最重要，别急着动手
   - 结果变差了？先搞清楚为什么：是方向不对，还是实现有 bug、超参没调好、评测口径变了？
   - 没想明白就先**调研**：读论文、查有没有现成的模块/手法、看同类问题别人怎么解的
   - **调研充分再动手**——跑错方向比多花时间查资料贵得多
3. **改方法 / 改代码**
4. **起实验验证**（先查空闲卡 → 保证会写 `DONE` → 调 `track_run` 登记）
5. **更新 `.auto/notes.md`**，然后结束这一轮

几个提醒：

- **别原地打转**：同一个想法试 2~3 次没效果就换方向，并把「试过、没用」写进死胡同
- **调研是工作的一部分**，不是拖延
- **死胡同比成功经验值钱**：跑几十轮以后，「什么不该再试」最能救命
- **不要只被动响应实验**：实验告诉你结果，但下一步往哪走得你自己想

## baseline 和迭代的区别

两件事共用同一个循环、同一份表，但**目标不同**，混着干会两边都做不好：

| | 迭代 | baseline |
|---|---|---|
| 目标 | 让我的方法持续变好 | 找到能对比的方法 → 改造到能跑 → 出数 |
| 调研 | 找新方法、新模块、新思路 | 找**有哪些方法可以拿来对比** |
| 主要障碍 | 下一步往哪走 | 这个 repo 能不能跑起来 |
| 结果 | 看趋势，写进 notes | 先把数**完整记下来**，之后再整理 |

**baseline 的起点是找方法**：我通常并不预先知道有哪些方法可以对比，
所以第一步是调研——读文献、找开源实现、选出候选。再逐个拉代码、改造、跑。
这一步和迭代的调研同样重要，只是**找的东西不同**（找"选谁比"，不是"怎么改"）。

**改造是主要工作量**：别人的代码往往跑不通（环境、接口、数据格式），
让它跑起来本身就是活儿。

**结果先不要结构化**：跑出来的数、用的配置、环境的坑、改了哪里——
全部写进实验记录的 md，**信息带全就行，格式随意**。等攒够一批再整理成对比表。
过早规范化会丢信息。

## 服务器与实验（最关键，一步步照做）

**在哪改代码你也决定**——服务器上直接改，或本地改完再上传（rsync / scp / git push）都行。
扩展不关心这件事，它只认 run 目录里的 `DONE`。

**怎么起实验你决定**——扩展不干涉、也不需要知道。下面 4 步必须全做：

> **为什么下面强调「服务器上的绝对路径」**：扩展每 60 秒 ssh 上去执行一句
> `test -f <路径>/DONE`，**它只认这一个信号**。相对路径、本地路径它都看不到。

**0. 摸清这台服务器的环境**

不同机器差别很大（裸机 / Slurm / Docker / 云 GPU 平台），起实验前探一下：

```bash
ssh {{SSH_ALIAS}} "which sbatch; which docker; which conda; nvidia-smi -L"
```

按探到的结果自己决定怎么起（需要 conda 就写在命令里：`conda run -n <环境名> python ...`）。
拿不到进程号就让它空着（Slurm 作业号、`docker` 都不算 pid），靠 `DONE` 也一样能等到。

> **判断错了不会报错，只会让实验永远没人等**——所以这步别省。

**1. 起实验，并尽量拿到进程号**

```bash
ssh {{SSH_ALIAS}} "cd /项目 && nohup python train.py > /输出目录/log.txt 2>&1 & echo \$!"
```

最后那个数字就是进程号（裸机才有；Slurm / Docker 见上表，留空）。

**2. 保证实验结束时会写 `DONE`**

- 最好**改训练代码**，让它跑完把指标写进 `<输出目录>/DONE`
- 或者命令末尾追加：`... ; touch <输出目录>/DONE`
- `DONE` 里放你的实验结果（指标、关键数字）——这是唯一的自动信号

**3. 立刻调 `track_run` 登记**

```
track_run(
  path="<输出目录的服务器绝对路径>",
  pid="<第 1 步的进程号>",
  track="iter",          # 或 baseline
  note="一句话：在试什么"
)
```

- pid **可选但强烈建议填**：填了崩了**立刻**通知，不填只能等 72 小时超时
  Slurm / Docker 拿不到 pid 就留空
- `track` / `note` **也强烈建议填**：它们会原样出现在唤醒消息里——
  ```
  - 完成：/data/.../exp12  [baseline / methodA 跑 SYSU]
  ```
  你被叫醒时一眼知道该读哪个 goal，不用回头猜这是哪个实验

**4. 之后不用管，扩展每 60 秒盯它**

> ⚠️ **漏掉第 2 步或第 3 步 → 这个实验永远不会有人等它，也不会有任何报错。**

## 收到通知后（完成 / 崩溃 / 超时）

1. 读那个路径下的 `DONE`（崩溃的话读它的日志定位原因）
2. **把那一行从 `.auto/runs.csv` 删掉**（不删会一直被查）
3. 按 `.auto/goal.md` 判断值不值，决定保留还是回滚
4. 更新 `.auto/notes.md`
5. 起下一个实验（回到上面 4 步）

## 每轮结束必须做

**1. 重写 `.auto/notes.md`**（不是追加，保持精简——它每轮都会被读）：

- **当前重心在哪一边**（迭代还是 baseline）、为什么 ← 双工作流时最重要
- 当前节点、正在验证什么、最近结果、下一步打算
- **死胡同**：明确不要再试的，及原因 ← 第二重要
- 环境坑

**2. 更新 `.auto/tree.md`**（累积记录，按需才读，所以可以详细）：

每试一个新方向就追加一个节点，写清：父节点、改动、**commit id**、结果、保留还是回退。

- **commit 建议真的提交**（`git commit -am "N5: ..."`）。只记 HEAD 但没提交的话，
  那个节点是**回不去**的——未提交的改动没法恢复。

## 尝试的结构（别原地打转）

迭代最容易在同一个想法上反复打转。用树来组织：

```
N1 基线
├─ N2 加注意力      ✓ 保留
│  ├─ N5 换骨干     ✗ 回退
│  └─ N6 调 loss    ✓ 保留   ← 当前
└─ N3 换数据增强    ✗ 回退
```

- 一个方向不行就**回到父节点**再试别的，别死磕
- 父节点也不行就**再往上退一层**（可能是更根本的假设错了）
- 同一模块可以并行试几种改法，各自一个分支

## 代码管理

- 不强制 commit，但**想回溯就必须提交**（见上面 tree.md）
- 决定回滚时 `git checkout <那个节点的 commit>`，并在 tree.md 里标 ✗ 回退
- 不要在一个失败上无限重试；试 2~3 次还不行就换方向，把原因写进死胡同

## 其他约定

- **起实验前必须先查显卡占用**，自己 ssh 查：
  `ssh {{SSH_ALIAS}} "gpustat"`（没装 gpustat 就用
  `nvidia-smi --query-gpu=index,memory.used,memory.total,utilization.gpu --format=csv`）
  只往**确认空闲**的卡上放实验——服务器上还有别人
- **并发上限见 `.auto/goal.md`**（多人共用的服务器务必设上限，不要占满）
- 不动别人的进程、文件、环境
- 登记超过 72 小时还没有 `DONE`，扩展会提醒——通常意味着崩了或忘了写 DONE

### SSH 使用纪律（排查问题必读）

- 排查服务器问题**可以自由用 ssh**（看日志、查进程、kill、nvidia-smi……），但必须走正门：
  `ssh {{SSH_ALIAS}} '<任意命令>'`（原生 ssh + `~/.ssh/config` 里配好的别名）
- 不要用**另一套环境里的 ssh**（比如 `wsl ssh`）——它有独立的钥匙和 known_hosts，会卡在指纹确认
- 也不要 `ssh 用户@裸IP`（绕过别名，同样卡交互确认）

## 调研

- 优先读本地 `refs/`（论文、repo 笔记）
- 需要查新东西时用联网搜索工具
- 外部模块/包在写进方案前先确认它真的存在

## 结果判读

- 多指标往往有权衡（例如主指标涨但副作用指标也涨），**由你判断值不值**，不要只看单一数字
- 具体看哪些指标、怎么算"变好"，按 `.auto/goal.md` 里我写的口径
- baseline 的结果**先把信息记全**：数、配置、环境坑、改了哪里，都写进实验记录的 md。
  别只留在某个 run 的 `DONE` 里——那样等于白跑。整理成对比表是之后的事

> 我可以随时打断你：打字后按 Enter 会立刻插入（steer），按 Esc 中止。
> 被打断时以我最新说的为准。
